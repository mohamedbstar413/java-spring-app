name: CI Pipeline

on:
    push:
        branches: [ main ]
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
              name: Checkout code
            - name: Run Script
              run: |
                echo "Running CI pipeline..."
                #echo the actor's name, the branch, the commit SHA, and the event type
                echo "Actor: ${{ github.actor }}"
                echo "Branch: ${{ github.ref }}"
                echo "Commit SHA: ${{ github.sha }}"
                echo "Event Type: ${{ github.event_name }}"
            - name: Installing java
              uses: actions/setup-java@v4
              with:
                distribution: 'temurin'
                java-version: '17'
            - name: Linting java code
              run: |
                #for each microservice, run the checkstyle and pmd plugins
                all_dirs = $(find . -type d -name *-service)
                for dir in $all_dirs; do
                  cd $dir
                  mvn checkstyle:check pmd:check
                  cd ..
                done